services:
  - type: web
    name: drayage-portal
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    autoDeploy: true
    healthCheckPath: /
